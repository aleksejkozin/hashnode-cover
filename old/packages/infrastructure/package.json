{
  "name": "@hashnode-cover/infrastructure",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "deploy": "pulumi up --stack prod -y",
    "destroy": "pulumi destroy --stack prod -y",
    "set:secret": "pulumi config set --stack prod --secret key value",
    "pulumi": "pulumi"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@types/node": "^14",
    "@pulumi/pulumi": "^3.20.0",
    "@pulumi/random": "^4.3.1",
    "@pulumi/azuread": "^5.11.0",
    "@pulumi/docker": "^3.1.0",
    "@pulumi/azure-native": "^1.49.0",
    "@pulumi/auth0": "^2.3.2"
  }
}

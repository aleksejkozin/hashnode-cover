# nx automatically loads .env* for NODE_* and NX_* environment variables
# Other environment variables should be load manually with env-cmd command

# These variables will be overwritten by system environment variables
# Concider them as defaults if the system variables are not presented

# An entry point for all other configurations
# We will use this name and "az login" to download other configs
APP_CONFIG_NAME=hashnodeconfigcb170967

# This will allow Docker support --mount cache
DOCKER_BUILDKIT=1

# We want node to crash on unhandled exceptions
# We want node to support source maps
# We want node to load ./startup.js for each instance
NODE_OPTIONS="--enable-source-maps --unhandled-rejections=strict --require ./startup.js $NODE_OPTIONS"

# Auth0 configs for NextJs
AUTH0_BASE_URL=http://localhost:3000
AUTH0_SECRET=thisisadebugappsecretjustarandomstringwowyoureadsofar
# Other AUTH0_* configurations avalible only during Pulumi deploy time
# They will be loaded from .env.global
